<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="CustomActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="android:colorAccent">@color/md_teal_900</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="background">@color/md_teal_900</item>
        <item name="android:statusBarColor">@color/md_teal_900</item>
        <!-- Support library compatibility -->
        <!--<item name="background">@drawable/actionbar_background</item>-->
    </style>

    <drawable name="actionbar_background">#ffff0000</drawable>
</resources>